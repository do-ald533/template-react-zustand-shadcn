import { MainHeader } from "@/components/common/MainHeader";
import { BreadcrumbType } from "../../../types/breadrumb";

const breadcrumbs: Array<BreadcrumbType> = [
	{
		name: "Home",
		href: "/",
	},
];

export const Home = () => {
	return (
		<main>
			
		</main>
	);
};
